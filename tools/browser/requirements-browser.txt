# DasLab WS-A browser/computer-use tool — optional deps (DAS-1548).
# Kept OUT of core requirements.txt so the engine stays server-free (ADR-0033
# TB-1, check_no_dead_runtime) and the tool is absent-by-default: install only
# where the browser sidecar actually runs.
#   pip install -r tools/browser/requirements-browser.txt
mcp>=1.2
# Real browser backend (wired in tools/browser/playwright_backend.py, behind the
# unchanged C8 action gate). Engaged only when DASLAB_BROWSER_BACKEND=playwright;
# unset => the stdlib reference backend runs and this dep is not needed.
# After install run:  python -m playwright install chromium
playwright>=1.45
#   browser-use>=0.1          # alternative driver (do not enable alongside playwright)
