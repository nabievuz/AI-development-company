# DasLab WS-D LENS optional observability deps (ADR-0036 / DAS-1573).
#
# Kept OUT of the core requirements.txt so the engine stays server-free and the
# flag-OFF exporter carries no runtime weight (design §1.3, mirrors
# tools/mcp_bridges/requirements-tools.txt). The CORE exporter
# (tools/observability/otlp_exporter.py) imports NONE of these — it builds OTLP/
# HTTP JSON with the stdlib and ships it via the stdlib `urllib` transport.
#
# Install ONLY on the in-tenant host that runs the lens, and only if you prefer
# the official OTLP proto exporter/SDK over the stdlib JSON transport:
#   pip install -r tools/observability/requirements-observability.txt
#
# opentelemetry-sdk>=1.27
# opentelemetry-exporter-otlp-proto-http>=1.27
#
# The self-host Langfuse instance itself is provisioned on the tenant VM (Q2) and
# is NOT a Python dependency; the exporter only needs its in-tenant OTLP endpoint
# (config/tenant_boundary.yaml `langfuse_observability`, http://127.0.0.1:3000).
